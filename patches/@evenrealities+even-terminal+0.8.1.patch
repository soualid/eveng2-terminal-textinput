diff --git a/node_modules/@evenrealities/even-terminal/dist/claude/provider.js b/node_modules/@evenrealities/even-terminal/dist/claude/provider.js
index e4a0172..0a3ce19 100644
--- a/node_modules/@evenrealities/even-terminal/dist/claude/provider.js
+++ b/node_modules/@evenrealities/even-terminal/dist/claude/provider.js
@@ -17,13 +17,10 @@ function findSessionFile(sessionId) {
     return null;
 }
 /** Read last line of a jsonl file. */
-function readLastLine(filePath) {
+function readLines(filePath) {
     try {
         const data = readFileSync(filePath, "utf8");
-        const lines = data.trimEnd().split("\n");
-        if (lines.length === 0)
-            return null;
-        return JSON.parse(lines[lines.length - 1]);
+        return data.trimEnd().split("\n");
     }
     catch {
         return null;
@@ -34,31 +31,46 @@ export function claudeSessionStatus(sessionId) {
     const file = findSessionFile(sessionId);
     if (!file)
         return "idle";
-    const last = readLastLine(file);
-    if (!last)
-        return "idle";
-    if (last.type === "last-prompt")
-        return "idle";
-    if (last.type === "system" && (last.subtype === "stop_hook_summary" || last.subtype === "turn_duration"))
-        return "idle";
-    if (last.type === "assistant" && last.message?.stop_reason && last.message.stop_reason !== "tool_use")
+    const lines = readLines(file);
+    if (!lines)
         return "idle";
-    if (last.type === "result")
-        return "idle";
-    if (last.type === "permission-mode")
-        return "idle";
-    if (last.type === "user") {
-        const c = last.message?.content;
-        const text = typeof c === "string" ? c : c?.[0]?.text ?? "";
-        if (text.includes("Request interrupted by user"))
+    // Newer Claude Code versions append timestamp-less metadata trailers
+    // (ai-title, mode, pr-link, ...) after the turn ends, so scan backward
+    // past them until a turn marker or a timestamped entry is found.
+    for (let i = lines.length - 1; i >= 0; i--) {
+        let last;
+        try {
+            last = JSON.parse(lines[i]);
+        }
+        catch {
+            continue;
+        }
+        if (!last)
+            continue;
+        if (last.type === "last-prompt")
             return "idle";
-    }
-    if (last.timestamp) {
+        if (last.type === "system" && (last.subtype === "stop_hook_summary" || last.subtype === "turn_duration"))
+            return "idle";
+        if (last.type === "assistant" && last.message?.stop_reason && last.message.stop_reason !== "tool_use")
+            return "idle";
+        if (last.type === "result")
+            return "idle";
+        if (last.type === "permission-mode" || last.type === "mode")
+            return "idle";
+        if (last.type === "user") {
+            const c = last.message?.content;
+            const text = typeof c === "string" ? c : c?.[0]?.text ?? "";
+            if (text.includes("Request interrupted by user"))
+                return "idle";
+        }
+        if (!last.timestamp)
+            continue;
         const ageMs = Date.now() - new Date(last.timestamp).getTime();
         if (ageMs > 120_000)
             return "idle";
+        return "busy";
     }
-    return "busy";
+    return "idle";
 }
 // ── SDK message → display turn helpers ────────────────
 function extractTextContent(content) {
diff --git a/node_modules/@evenrealities/even-terminal/dist/claude/session.js b/node_modules/@evenrealities/even-terminal/dist/claude/session.js
index a98fa8d..2baa4a4 100644
--- a/node_modules/@evenrealities/even-terminal/dist/claude/session.js
+++ b/node_modules/@evenrealities/even-terminal/dist/claude/session.js
@@ -162,7 +162,7 @@ export class ClaudeSession {
                     queue.splice(idx, 1);
                 resolve(value);
             };
-            const timer = setTimeout(() => finish(defaultValue), timeoutMs);
+            const timer = timeoutMs ? setTimeout(() => finish(defaultValue), timeoutMs) : null;
             const onAbort = () => finish(defaultValue);
             signal.addEventListener("abort", onAbort, { once: true });
             queue.push(entry);
@@ -468,7 +468,7 @@ export class ClaudeSession {
             options: permissionOptions,
             suggestions: suggestions ?? null,
         });
-        const result = await this.waitForUser(this.pendingPermissions, signal, 60000, { allow: false, allowAlways: false });
+        const result = await this.waitForUser(this.pendingPermissions, signal, 0, { allow: false, allowAlways: false });
         let sdkDecision;
         if (result.allowAlways) {
             this.alwaysAllowedTools.add(toolName);
diff --git a/node_modules/@evenrealities/even-terminal/dist/codex/session.js b/node_modules/@evenrealities/even-terminal/dist/codex/session.js
index 5280b23..a243921 100644
--- a/node_modules/@evenrealities/even-terminal/dist/codex/session.js
+++ b/node_modules/@evenrealities/even-terminal/dist/codex/session.js
@@ -19,7 +19,7 @@ export class CodexSession {
     pendingPermissions = [];
     externallyResolvedPermissions = new Map();
     pendingQuestions = [];
-    permissionTimeoutMs = 60000;
+    permissionTimeoutMs = 0;
     questionTimeoutMs = 120000;
     assistantText = "";
     turnStartedAt = 0;
@@ -517,9 +517,9 @@ export class CodexSession {
         if (!pending || pending.visible)
             return;
         pending.visible = true;
-        pending.timer = setTimeout(() => {
+        pending.timer = this.permissionTimeoutMs ? setTimeout(() => {
             this.autoRespondPermission(String(pending.requestId), "deny");
-        }, this.permissionTimeoutMs);
+        }, this.permissionTimeoutMs) : null;
         const description = summarizeCodexRequest(pending.params, pending.method);
         this.send({
             type: "permission_request",
